{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "preview": "lerna --scope=site run start"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}